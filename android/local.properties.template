# Android SDK和Flutter路径配置模板
# 请根据实际安装路径修改以下配置

# Android SDK路径 (必须)
sdk.dir=YOUR_ANDROID_SDK_PATH

# NDK路径 (可选，如果需要NDK支持)
# ndk.dir=YOUR_NDK_PATH

# Flutter SDK路径 (必须)
flutter.sdk=YOUR_FLUTTER_SDK_PATH

# Flutter构建配置 (通常不需要修改)
flutter.buildMode=debug
flutter.versionName=1.0.0
flutter.versionCode=1

# 示例路径格式:
# Windows: C:\\Android\\Sdk
# macOS/Linux: /Users/username/Android/Sdk
# 注意: Windows路径需要使用双反斜杠 \\ 或正斜杠 /